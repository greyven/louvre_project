{# app/Resources/views/command.html.twig #}
{% extends 'base.html.twig' %}

{% block body %}

    <br/>
    <br/>
    <div class="row">
        <div class="col-md-offset-3 col-md-6 col-sm-offset-2 col-sm-8">
            <div class="customWell">
                <h1 class="text-center">Réservation de billets</h1>
            </div>
        </div>
    </div>
    <br/>

    <div class="row">
        <div class="col-md-offset-2 col-md-8 col-sm-offset-1 col-sm-10  col-xs-12">

            <div class="well">
                {{ form_start(commandForm) }}
                <div class="row">
                    <div class="col-sm-3 col-xs-12">
                        {{  form_row(commandForm.fullDay) }}
                    </div>
                    <div class="col-sm-9 col-xs-12">
                        {{ form_row(commandForm.visitDate) }}
                    </div>
                </div>
                <div class="row">
                    <div class="col-sm-4 col-xs-12">
                        {{ form_row(commandForm.numberOfTickets) }}
                    </div>
                    <div class="col-sm-8 col-xs-12">
                        {{ form_row(commandForm.visitorEmail) }}
                    </div>
                </div>
                <div class="row">
                    <div class="col-xs-12">
                        {{ form_errors(commandForm) }}
                    </div>
                </div>

                <div class="row">
                    <div class="col-xs-12">
                        <div class="horizAlignCenter">
                            <input type="submit" value="Valider" class="btn btn-default" />
                        </div>
                    </div>
                </div>
                {{ form_end(commandForm) }}
            </div>
        </div>
    </div>
{% endblock %}